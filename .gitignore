node_modules/
.expo/
.expo-shared/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
dist/

# Native folders (EAS generates these)
android/
ios/

# Test coverage
coverage/

# Build output
lib/

# Documentation (not needed for builds)
docs/
critical_analysis/

# Claude Code config
.claude/

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# Environment variables (when real keys are added)
.env.local
.env.production

# Firebase config (when real keys are added)
src/services/firebase/config.production.ts